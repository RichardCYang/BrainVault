# BrainVault 심층 분석 및 수정 결과

검토일: 2026-08-05

## 결론

첨부 프로젝트의 최근 개발 방향은 비동기 작업을 계정·페이지·최신 사용자 의도에 묶고, 늦게 도착한 응답이 현재 상태를 덮지 못하게 하는 것입니다. 기존 코드의 프로필, 아바타, 페이지 커버, 아이콘 선택기, 검색, 버전 이력, 협업 처리에는 이 원칙이 반영되어 있었지만 계정 보안 화면, 공유 목록, 기본 문서 전환에는 같은 보호가 빠져 있었습니다.

아래 세 결함을 결정적으로 재현하고 수정했습니다.

1. **계정 경계 정보 노출 및 상태 오염**: 이전 계정의 로그인 이력, 패스키 이름, MFA 상태, TOTP 설정 비밀키가 다음 계정 화면에 남거나 늦은 응답으로 덮일 수 있었습니다.
2. **공유 목록의 잘못된 페이지 결합**: 페이지 A의 느린 공유 목록이 페이지 B 대화상자에 표시될 수 있었고, 이 상태에서 제거하면 페이지 B URL에 페이지 A 사용자 ID가 결합될 수 있었습니다.
3. **문서 전환 최신 의도 역전**: 먼저 클릭한 느린 문서 응답이 나중에 클릭한 문서보다 늦게 도착하면 최종 화면을 다시 이전 문서로 바꿀 수 있었습니다.

인증 초기화 시 남을 수 있던 페이지 버전 이력·페이지 커버 관련 대화상자도 함께 닫도록 수정했습니다.

## 주요 수정

- 계정 보안 비동기 작업을 계정 ID와 요청 세대에 결합했습니다.
- 설정 닫기·로그아웃·401 인증 초기화에서 로그인 이력, MFA/패스키 상태, TOTP 토큰·QR·비밀키를 명시적으로 지웁니다.
- WebAuthn 패스키 등록의 옵션 요청, 브라우저 인증기 처리, 서버 검증 사이마다 현재 계정을 다시 확인하고, 상태 조회가 동시에 끝나도 등록 버튼이 중복 활성화되지 않게 했습니다.
- 공유 목록 요청은 캡처한 페이지 ID, 대화상자 상태, 소유권, 요청 세대가 모두 일치할 때만 반영합니다.
- 홈·컬렉션·문서 전환에 공통 탐색 세대를 적용해 가장 최근 전환만 상태를 변경하며, 이미 대체된 요청의 오류 메시지도 현재 화면에 표시하지 않습니다.
- 취약 구현과 수정 구현을 동일 지연 순서로 비교하는 독립 재현 스크립트 3개와 회귀 테스트 6개를 추가했습니다.

## 검증 결과

- 의존성 없는 Node 내구성 테스트: **141/141 통과**
- 신규 재현·회귀 테스트: **6/6 통과**
- 잠금파일 레지스트리 검사: **346개 URL 통과**
- 데이터 손실 방지 검증: 통과
- 협업·프로토콜·소스 검증: 통과, **261개 파일 구문 확인**
- 보안 강화 검증: 통과
- 수정 JavaScript 구문 검사: 통과

## 실행 환경 제한

프로젝트는 Node.js `^22.23.2 || ^24.18.1 || >=26.5.1`과 npm `engine-strict`를 요구하지만 검토 환경은 Node.js 22.16.0이었습니다. 강제 설치 검증도 샌드박스 패키지 미러에 잠금된 `zod@3.25.76`, `yjs@13.6.31` 아티팩트가 없어 완료되지 않았습니다. 따라서 TypeScript 전체 빌드와 Vitest 의존성 기반 테스트는 실행 완료로 보고하지 않습니다. `node_modules`는 결과물에 남기지 않았습니다.

## 재현 명령

```bash
npm run reproduce:account-security-auth-boundary
npm run reproduce:share-dialog-request-race
npm run reproduce:workspace-navigation-race
npm run test:durability
npm run verify:data-loss
npm run verify:collaboration
npm run verify:security
```

## `.git` 보존

`.git` 폴더는 삭제·초기화·정리하지 않았습니다. 최종 압축 전 원본 추출 기준본과 전체 `.git` 파일 목록 및 SHA-256 해시를 비교합니다.

상세 기술 문서: `docs/data-loss/2026-08-05/account-security-and-ui-request-scope.md`
