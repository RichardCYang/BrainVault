# BrainVault 심층 분석 및 수정 결과

검토일: 2026-08-05

## 결론

첨부 프로젝트의 개발 방향은 **비동기 결과를 시작 당시의 계정·페이지·최신 사용자 의도에 묶고, 인증 경계와 데이터 보존 경계에서는 오래된 작업이 현재 상태를 바꾸지 못하게 하는 것**으로 판단됩니다. 기존 코드의 초안 보존, Yjs 협업, 프로필·아바타, 페이지 커버, 검색, 버전 이력 등에는 이 원칙이 폭넓게 반영되어 있었습니다.

전체 흐름을 지연 순서와 응답 유실까지 통제해 재검토한 결과, 앞서 수정된 인증·백업·편집 잠금 경계 결함군에 더해 페이지 생성 멱등성, 생성 UI 직렬화, 인증 세대가 바뀐 뒤의 완료 효과라는 세 결함군을 추가로 재현하고 수정했습니다.

1. **계정 보안 상태의 인증 경계 누출**: 이전 계정의 로그인 이력, 패스키 이름, MFA 상태, TOTP 설정 비밀키가 다음 계정 화면에 남거나 늦은 응답으로 덮일 수 있었습니다.
2. **공유 목록의 잘못된 페이지 결합**: 페이지 A의 느린 공유 목록이 페이지 B 대화상자에 표시되고, 잘못된 페이지/사용자 조합으로 제거 요청이 만들어질 수 있었습니다.
3. **문서 전환 최신 의도 역전**: 먼저 클릭한 느린 문서 응답이 나중 클릭보다 늦게 도착하면 최종 화면을 이전 문서로 되돌릴 수 있었습니다.
4. **부팅 세션·수동 로그인·MFA 완료 경합**: 부팅 중 쿠키 세션 복원이 더 최신 로그인 흐름을 넘어 옛 사용자를 남길 수 있었고, 로그인/MFA 요청의 중복 완료 순서가 브라우저 세션과 UI를 불일치시킬 수 있었습니다.
5. **백업 가져오기·내보내기의 계정 경계 오염**: 계정 전환 뒤 이전 계정 백업이 다운로드되거나, 이전 가져오기 응답과 페이지 목록이 현재 계정 상태를 덮을 수 있었습니다.
6. **이전 계정 작업의 잠금 해제 역전**: 오래된 `finally`가 새 계정의 편집 잠금 깊이를 감소시키거나, 이전 전환 리스 참조가 새 계정을 계속 잠긴 상태로 보이게 할 수 있었습니다.
7. **응답 유실 뒤 페이지 중복 생성**: `POST /api/pages`가 커밋된 뒤 응답만 유실되면 같은 사용자 의도를 재시도할 때 두 번째 페이지가 영구 생성될 수 있었습니다.
8. **빠른 중복 클릭과 오래된 생성 완료**: 생성 버튼들이 공통 busy 상태를 공유하지 않아 병렬 요청이 시작될 수 있었고, 계정 전환 뒤 이전 계정의 생성 응답이 현재 목록·화면을 바꿀 수 있었습니다.
9. **첨부 다운로드와 401의 인증 경계 누출**: 이전 계정에서 시작한 비공개 첨부가 새 계정에서 자동 다운로드되거나, 이전 요청의 늦은 `401`이 새 로그인 세션을 초기화할 수 있었습니다.

## 추가 수정 내용

### 인증 흐름

- 부팅 세션 복원을 인증 작업 세대에 결합하고 각 비동기 단계 뒤 현재 작업인지 검사합니다.
- 부팅 복원이 중간 상태를 기록한 뒤 대체되면, 자신이 기록한 사용자 상태만 롤백하며 이미 완료된 새 로그인은 건드리지 않습니다.
- 같은 탭에서 로그인·가입·MFA 검증이 중복 제출되지 않도록 직렬화했습니다.
- 인증 요청 중 로그인 모드 전환, 해시 전환, MFA 방식 전환 및 인페이지 취소를 막아 응답 순서가 브라우저 관리 세션 쿠키와 충돌할 가능성을 줄였습니다.
- 로그인 성공 뒤 초기 워크스페이스 로딩을 별도 계정 세대에 결합했습니다. 로그아웃·401·계정 변경 뒤 늦게 도착한 페이지 목록은 반영되지 않습니다.
- 초기 페이지 목록 로딩이 끝나거나 안전하게 실패하기 전에는 대화형 워크스페이스를 노출하지 않습니다. 페이지 로딩만 실패한 경우에는 유효한 로그인 세션을 유지하고 오류 상태를 표시합니다.

### 백업 전송

- 내보내기·가져오기를 시작 계정 ID와 요청 세대에 결합했습니다.
- 전송 중 설정 창의 일반 닫기를 막되, 인증 초기화에서는 강제로 닫고 작업 세대를 무효화합니다.
- 내보내기는 전체 응답 길이 검증 뒤에도 계정을 다시 확인한 후에만 다운로드 링크를 만듭니다.
- 가져오기는 서버 응답 사용자 ID가 시작 계정과 같은지 검증하고, 페이지 목록을 로컬 값으로 받은 뒤 현재 계정 확인 후 한 번에 반영합니다.
- 마지막에만 검사하면서 이미 전역 상태를 바꾸던 `loadPages()` 사용을 제거했습니다.

### 편집 잠금과 전환 리스

- 편집 잠금 획득 시 인증 경계 세대를 함께 저장합니다.
- 인증 초기화는 잠금 세대를 증가시키며, 이전 세대의 `finally`는 새 계정 잠금을 해제할 수 없습니다.
- 현재 탭이 소유한 활성 전환 리스는 인증 초기화에서 명시적으로 해제하고 참조를 지웁니다.

### 페이지 생성 멱등성과 UI 직렬화

- `page_create_mutations` 영수증 테이블을 추가하고 `(owner_id, mutation_id)`를 기본키로 사용합니다.
- 페이지, 초기 블록, 태그, 생성 버전 이력과 영수증을 한 SQL 트랜잭션에서 처리합니다.
- 같은 요청 본문과 mutation ID의 재전송은 원래 페이지를 반환하며, 다른 본문 재사용은 `409 MUTATION_ID_REUSED`로 거절합니다.
- 원래 페이지가 영구 삭제된 뒤 늦게 재시도되면 새 페이지를 만들지 않고 `409 PAGE_CREATE_REPLAY_UNAVAILABLE`로 거절합니다.
- 컬렉션/홈/사이드바 생성 버튼은 공통 busy 상태를 사용하고, 애매한 실패는 같은 mutation ID로 한 번 자동 재시도합니다.
- 서버 생성 성공 뒤 목록 재조회나 화면 이동이 실패해도 작업 영수증을 즉시 잊지 않고 전체 UI 완료 시점까지 유지하여, 다음 시도가 새 페이지를 중복 생성하지 않게 했습니다.

### 인증 세대가 바뀐 뒤의 완료 효과

- API 요청은 시작 시 인증 세대와 계정 키를 캡처하며, 늦은 `401`은 동일한 세션일 때만 인증을 초기화합니다.
- 비밀번호 변경으로 서버 쿠키가 교체되면 계정 ID가 같더라도 인증 세대를 증가시켜, 이전 자격 증명으로 시작된 응답이 새 세션을 초기화하지 못하게 했습니다.
- 페이지 생성의 응답, 전체 목록 재조회, 페이지/컬렉션 이동은 각 단계 뒤 시작 계정이 여전히 현재 계정인지 확인합니다.
- 첨부 응답과 Blob 변환 뒤에도 다시 확인하며, 마지막 검사 전에는 다운로드 클릭을 만들지 않습니다.
- 인증 초기화는 생성 busy 상태와 같은 세션의 재시도 맵을 함께 비웁니다.

## 재현 및 테스트

추가 재현 명령:

```bash
npm run reproduce:auth-data-lock-boundary
npm run reproduce:page-create-auth-boundary
```

추가 회귀 범위:

- `tests/auth-data-lock-boundary.node.test.mjs`
- `tests/auth-session-bootstrap.node.test.mjs`의 부팅 복원 소유권·롤백 4개 사례
- `tests/page-create-auth-boundary.node.test.mjs`의 영수증, 마이그레이션, UI 인증 범위, 독립 재현 5개 사례
- 백업 및 페이지 생성 변경을 반영한 데이터 손실 검증기와 정적 검증

## 최종 검증 결과

- 의존성 없는 Node 내구성 테스트: **154/154 통과**
- 이번 페이지 생성·다운로드 추가 회귀 테스트: **5/5 통과**
- 잠금파일 레지스트리 검사: **346개 URL 통과**
- 데이터 손실 방지 검증: 통과
- 협업·프로토콜·소스 검증: 통과, **266개 파일 구문 확인**
- 보안 강화 검증: 통과, 의존성 없는 보안 회귀 **11/11 통과**
- 수정 JavaScript 및 재현 스크립트 구문 검사: 통과

## 실행 환경 제한

프로젝트는 Node.js `^22.23.2 || ^24.18.1 || >=26.5.1`과 npm `engine-strict`를 요구하지만 검토 환경은 Node.js 22.16.0이었습니다. 강제 설치 검증도 샌드박스 패키지 미러에서 잠금된 `zod@3.25.76` 아티팩트를 찾지 못해 완료되지 않았습니다.

따라서 TypeScript 전체 빌드와 의존성 기반 Vitest 전체 테스트는 실행 완료로 보고하지 않습니다. `node_modules`는 결과물에 포함하지 않습니다.

## 전체 재현·검증 명령

```bash
npm run reproduce:account-security-auth-boundary
npm run reproduce:share-dialog-request-race
npm run reproduce:workspace-navigation-race
npm run reproduce:auth-data-lock-boundary
npm run reproduce:page-create-auth-boundary
npm run test:durability
npm run lockfile:check
npm run verify:data-loss
npm run verify:collaboration
npm run verify:security
```

## `.git` 보존

`.git` 폴더는 삭제·초기화·정리하지 않았습니다. 최종 압축본은 원본 업로드 ZIP의 `.git` 전체 경로, 크기, SHA-256 해시와 동일한지 비교한 뒤 제공합니다.

상세 기술 문서:

- `docs/data-loss/2026-08-05/account-security-and-ui-request-scope.md`
- `docs/data-loss/2026-08-05/auth-data-and-lock-boundary-review.md`
- `docs/data-loss/2026-08-05/page-create-idempotency-and-download-boundary.md`
