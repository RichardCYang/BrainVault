// Browser-test/offline fallback. The server overrides /runtime-config.js at runtime
// so deployments receive the value configured by AI_CHAT_ANSWER_MAX_LENGTH.
export const AI_CHAT_ANSWER_MAX_LENGTH = 50_000;
